# QIconPicker Docs

This package contains the QIconPicker documentation, examples, and demo site.

```bash
pnpm install
pnpm build:ui
pnpm build:docs
pnpm --filter docs dev
```

The production site is emitted to `packages/docs/dist/spa`.
